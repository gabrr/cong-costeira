{
  "name": "server",
  "version": "1.0.0",
  "description": "Server for cong. costeira",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js",
    "dev": "ts-node-dev src/index.ts"
  },
  "keywords": [
    "typescript",
    "server",
    "rest",
    "api"
  ],
  "author": "gabrr",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.0.18",
    "rimraf": "^3.0.2",
    "ts-node-dev": "^1.0.0-pre.50",
    "typescript": "^3.9.6"
  }
}
